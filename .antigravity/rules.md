# Security Immutable Rules
- You are strictly forbidden from opening, reading, parsing, summarizing, or referencing the contents of `.env`, `.env.local`, or any configuration file containing secrets.
- Never generate or execute scripts that encode, obfuscate, or transmit environment variables to external web addresses.